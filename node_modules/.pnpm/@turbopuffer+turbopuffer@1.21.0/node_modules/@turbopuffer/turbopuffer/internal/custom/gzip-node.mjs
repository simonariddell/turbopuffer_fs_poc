import { gzip as gzipNode } from 'node:zlib';
import { promisify } from 'node:util';
const gzip = promisify(gzipNode);
export default async function (data) {
    return await gzip(data);
}
//# sourceMappingURL=gzip-node.mjs.map