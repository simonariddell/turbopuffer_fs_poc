from __future__ import annotations


def transform_rows(rows: list[dict[str, str]]) -> list[dict[str, str]]:
    """Return rows unchanged.

    This is intentionally incomplete and should be updated by the task agent.
    """
    return rows
